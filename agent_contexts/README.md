# Generated agent contexts

These files are optional reference material generated by `bun repomix`. Use `rg` and the checked-out
source as the source of truth, then open only the smallest context relevant to the task. Regenerate
contexts after source changes with `bun repomix`.
