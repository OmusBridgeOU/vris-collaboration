# 提供フレーム

このディレクトリへ正方形のPNG画像を追加すると、ビルド時にフレーム一覧へ自動追加されます。

画像は70mmの編集キャンバス全面へ固定表示され、位置や比率は利用者が変更できません。中央の写真を見せる部分は透明にしてください。

ファイル名は重複しない `snake_case` を使用し、公開・印刷利用の許諾を確認した素材だけを追加してください。例えば `sample_frame.png` はフレーム一覧に `sample_frame` と表示されます。

クレジットページは提供PNGスタンプだけを対象とするため、このディレクトリのフレームは表示されません。
